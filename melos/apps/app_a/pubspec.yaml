name: app_a
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  app_core:
    path: ../../packages/app_core
  http_album_repository:
    path: ../../packages/http_album_repository
  widgets_library:
    path: ../../packages/widgets_library

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true